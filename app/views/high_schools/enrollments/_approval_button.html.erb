<%= form_for [@high_school, enrollment], data: {remote: true, realtime: "submit"} do |f| %>
  <%= f.hidden_field :high_school_approved, value: true %>
  <%= f.submit "Approve", :class => 'secondary_btn' %>
<% end %>
